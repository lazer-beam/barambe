import React from 'react'
import { Menu, Icon } from 'semantic-ui-react'

// export const MenuItem = ({ icon, label }) => (<Menu.Item name={icon}><Icon name={icon} />{label}</Menu.Item>)
export default ({ icon, label }) => (<Menu.Item name={icon}><Icon name={icon} />{label}</Menu.Item>)
